[package]
name = "features"
version = "0.1.0"
edition = "2021"

[dependencies]

[features]
default = ["something"] # Features are disabled by default
something = []
doit = []
all = ["something", "doit"]
